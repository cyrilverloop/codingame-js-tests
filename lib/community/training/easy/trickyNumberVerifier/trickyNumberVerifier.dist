/**
 * The "Tricky number verifier" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/tricky-number-verifier}
 */
function execute(readline) {
    const N = parseInt(readline());
    for (let i = 0; i < N; i++) {
        const NUMBER = readline();
    }
    for (let i = 0; i < N; i++) {

        // Write an answer using console.log()
        // To debug: console.error('Debug messages...');

        console.log('answer');
    }
}

export { execute };
