/**
 * The "Unique prefixes" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/unique-prefixes}
 */
function execute(readline) {
    const N = parseInt(readline());
    for (let i = 0; i < N; i++) {
        const word = readline();
    }
    for (let i = 0; i < N; i++) {

        // Write an answer using console.log()
        // To debug: console.error('Debug messages...');

        console.log('<word_prefix>');
    }
}

export { execute };
