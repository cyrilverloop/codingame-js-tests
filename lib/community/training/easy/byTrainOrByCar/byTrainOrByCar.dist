/**
 * The "By train or by car ?" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/by-train-or-by-car}
 */
function execute(readline) {
    const T = readline();
    const N = parseInt(readline());
    for (let i = 0; i < N; i++) {
        const FNAME = readline();
    }

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('answer');
}

export { execute };
