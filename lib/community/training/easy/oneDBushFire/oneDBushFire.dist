/**
 * The "1D bush fire" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/1d-bush-fire}
 */
function execute(readline) {
    const N = parseInt(readline());
    for (let i = 0; i < N; i++) {
        const line = readline();
    }
    for (let i = 0; i < N; i++) {

        // Write an answer using console.log()
        // To debug: console.error('Debug messages...');

        console.log('answer');
    }
}

export { execute };
