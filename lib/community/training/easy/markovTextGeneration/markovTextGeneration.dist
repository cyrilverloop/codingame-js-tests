/**
 * The "Markov text generation" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/markov-text-generation}
 */
function execute(readline) {
    const t = readline();
    const d = parseInt(readline());
    const l = parseInt(readline());
    const s = readline();

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('fish is bad and');
}

export { execute };
