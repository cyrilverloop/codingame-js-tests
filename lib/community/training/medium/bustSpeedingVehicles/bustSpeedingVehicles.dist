/**
 * The "Bust speeding vehicles" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/bust-speeding-vehicles}
 */
function execute(readline) {
    const L = parseInt(readline());
    const N = parseInt(readline());
    for (let i = 0; i < N; i++) {
        const R = readline();
    }

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('result');
}

export { execute };
