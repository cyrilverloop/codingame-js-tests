/**
 * The "Circular automation, the period of chaos" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/circular-automation-the-period-of-chaos}
 */
function execute(readline) {
    const lineLength = parseInt(readline());
    const maxIter = parseInt(readline());
    const ruleNumber = parseInt(readline());

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('0');
}

export { execute };
