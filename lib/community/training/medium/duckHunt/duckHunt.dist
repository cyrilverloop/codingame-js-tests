/**
 * The "Duck hunt" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/duck-hunt}
 */
function execute(readline) {
    const w = parseInt(readline());
    const h = parseInt(readline());
    for (let i = 0; i < h; i++) {
        const row = readline();
    }
    for (let i = 0; i < h; i++) {
        const row = readline();
    }

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('ID x y');
}

export { execute };
