/**
 * The "Drug interactions" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/drug-interactions}
 */
function execute(readline) {
    const N = parseInt(readline());
    for (let i = 0; i < N; i++) {
        const s = readline();
    }

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('answer');
}

export { execute };
