/**
 * The "Box of cigars" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/box-of-cigars}
 */
function execute(readline) {
    const N = parseInt(readline());
    for (let i = 0; i < N; i++) {
        const LNT = parseInt(readline());
    }

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('2');
}

export { execute };
