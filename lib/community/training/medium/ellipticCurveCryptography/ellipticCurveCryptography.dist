/**
 * The "Elliptic curve cryptography" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/elliptic-curve-cryptography}
 */
function execute(readline) {
    const N = parseInt(readline());
    for (let i = 0; i < N; i++) {
        const k = readline();
    }
    for (let i = 0; i < N; i++) {

        // Write an answer using console.log()
        // To debug: console.error('Debug messages...');

        console.log('answer');
    }
}

export { execute };
