/**
 * The "Flood fill example" puzzle.
 * @see {@link https://www.codingame.com/ide/puzzle/flood-fill-example}
 */
function execute(readline) {
    const W = parseInt(readline());
    const H = parseInt(readline());
    for (let i = 0; i < H; i++) {
        const line = readline();
    }

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('answer');
}

export { execute };
